[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "the-eye"
version = "0.1.0"
description = "Deep URL Intelligence Scanner (CLI)"
requires-python = ">=3.8"

[project.scripts]
theeye = "theeye.cli:main"

